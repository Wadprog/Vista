import React from "react";
import { View, styles } from "react-native";

const CategoryPicker = ({ title, onPress }) => {
  return <View style={styles.container}></View>;
};

const styles = StyleSheet.create({
  container: {},
});
export default CategoryPicker;
